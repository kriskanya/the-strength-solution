# Deploys to Google Cloud Run on every push to main.
#
# Repository setup:
# - Secret: GCP_SA_KEY — JSON key for a service account with permission to deploy
#   to Cloud Run and run Cloud Build (e.g. roles/run.admin, roles/cloudbuild.builds.editor,
#   roles/iam.serviceAccountUser on the runtime SA, and access to the referenced secrets).
# - Variable: GCP_PROJECT — the-strength-solution-501103

name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to Cloud Run
        env:
          GCP_PROJECT: ${{ vars.GCP_PROJECT }}
        run: |
          set -euo pipefail
          DEPLOY_CMD=(gcloud run deploy the-strength-solution-web
            --source .
            --region us-central1
            --add-cloudsql-instances the-strength-solution-501103:us-central1:the-strength-solution-prod-db
            --service-account cloud-run-web@the-strength-solution-501103.iam.gserviceaccount.com
            --allow-unauthenticated
            --set-build-env-vars "POSTGRES_URL=postgresql://build:build@localhost:5432/build"
            --set-env-vars "NEXTAUTH_URL=https://thestrengthsolution.app,CRON_SERVICE_ACCOUNT_EMAIL=cron-scheduler@the-strength-solution-501103.iam.gserviceaccount.com,CRON_OIDC_AUDIENCE=https://thestrengthsolution.app/api/internal/rebuild-cohort-proficiencies"
            --set-secrets "POSTGRES_URL=POSTGRES_URL:latest,NEXTAUTH_SECRET=NEXTAUTH_SECRET:latest,GOOGLE_CLIENT_ID=GOOGLE_CLIENT_ID:latest,GOOGLE_CLIENT_SECRET=GOOGLE_CLIENT_SECRET:latest,FACEBOOK_CLIENT_ID=FACEBOOK_CLIENT_ID:latest,FACEBOOK_CLIENT_SECRET=FACEBOOK_CLIENT_SECRET:latest")
          if [ -n "${GCP_PROJECT}" ]; then
            DEPLOY_CMD+=(--project "${GCP_PROJECT}")
          fi
          "${DEPLOY_CMD[@]}"
